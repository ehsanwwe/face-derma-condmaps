# CPU-only runtime deps for the FastAPI service (server.py).
# torch / torchvision are installed separately in the Dockerfile from the CPU wheel index.
numpy==2.2.6
opencv-python-headless==4.12.0.88
onnxruntime==1.22.0
insightface==0.7.3
pillow==11.3.0
scikit-image==0.25.2
pyyaml
fastapi
uvicorn[standard]
python-multipart
